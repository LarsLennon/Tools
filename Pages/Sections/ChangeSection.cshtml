@page
@model ToolSmukfest.Pages.Sections.ChangeSectionModel

@{
    ViewData["Title"] = "ChangeSection";
}

<h1>ChangeSection</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Member) {
        <tr>
        </tr>
}
    </tbody>
</table>
